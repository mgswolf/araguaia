<%= provide :breadcrumb do %>
    <li><%= link_to t('admin.jobs'), admin_jobs_path %><span class="divider">/</span></li>
    <li class="active"><%= t('jobs.edit.title') + ": #{@job.title} " %></li>
<% end %>
<div class="page-header">
  <h2><%= t('jobs.edit.title') + ": #{@job.title} " %></h2>
</div>
<%= render :partial => 'form' %>
